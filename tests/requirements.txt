# Test-only dependencies.
#   pip install -r app/requirements.txt -r tests/requirements.txt
#
# Runtime dependencies live with their layer: retrieval/requirements.txt (retrieval)
# and app/requirements.txt (the Serve layer, which pulls in retrieval/).

pytest>=8.0.0
httpx>=0.27.0        # required by fastapi.testclient.TestClient
