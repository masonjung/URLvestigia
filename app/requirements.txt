# Serve layer dependencies.
#   pip install -r app/requirements.txt
#
# Pulls in the AI layer because server.py imports urlvestigia. Installing this file
# therefore gives you everything needed to run the accelerator:
#   make install  →  make dev
#
# The Lakehouse layer (data/db.py) needs nothing — sqlite3 is stdlib.

-r ../retrieval/requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9    # required by FastAPI to parse form posts
